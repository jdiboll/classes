class Dog {
	
	constructor (dogParams) {
		dogParams = dogParams || {};
		this.color = black;
		this.hungry = dogParams.hungry;
		this.status = normal;
		this.owner = dogParams.owner;
}		
}
  //export default Dog;